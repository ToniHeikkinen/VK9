package com.example.studentregister;

import android.view.View;
import android.widget.ImageView;
import android.widget.TextView;

import androidx.annotation.NonNull;
import androidx.recyclerview.widget.RecyclerView;
/*
public class studentViewHolder extends RecyclerView.ViewHolder {
    ImageView imageView;
    TextView firstName,lastName, email, degreeProgram;

    public studentViewHolder(@NonNull View itemView) {
        super(itemView);
        imageView=itemView.findViewById(R.id.imageView);
        firstName=itemView.findViewById(R.id.txtStudentFirstName);
        lastName=itemView.findViewById(R.id.txtStudentLastName);
        email=itemView.findViewById(R.id.txtEmail);
        degreeProgram=itemView.findViewById(R.id.txtDegreeProgram);
    }
}
*/
